spring:
  application:
    name: micro-weather-config-client
  cloud:
    config:
      profile: dev #配置版本
      uri: http://localhost:8888/

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/   
      